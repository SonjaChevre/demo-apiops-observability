apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: tyk-gateway-conf
  namespace: argocd
spec:
  destination:
    namespace: tyk
    server: https://kubernetes.default.svc
  project: apis
  source:
    repoURL: 'https://github.com/SonjaChevre/demo-apiops-observability.git'
    path: staging/tyk-gateway-conf
    targetRevision: HEAD
  syncPolicy:
    syncOptions:
    - CreateNamespace=true

    automated:
      selfHeal: true
      prune: true
